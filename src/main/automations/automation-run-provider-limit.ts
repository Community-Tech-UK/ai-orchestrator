/**
 * Re-export of the provider-limit classifier, which moved to a neutral module
 * so the Plan Queue can share it without importing from automations/.
 */
export * from '../cli/final-output-provider-limit';
