# Dependencies
node_modules/

# Build outputs
dist/
out/
release/
!src/main/release/
!src/main/release/**

# Visual QA screenshot artifacts (docs/visual-qa-checklist.md)
visual-qa/

# Angular
.angular/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Claude attachments (temporary files for file uploads)
.claude-attachments/
# Session archive data lives in the repo root; keep feature folders named
# "archive" trackable under src/.
/archive/

# Claude Code settings
.claude/

# Environment
.env
.env.local

# Benchmark results (large, generated data)
benchmarks/orchestrator-benchmark/results/
benchmarks/orchestrator-benchmark/dist/

# WS16 local-personal retrieval queries (references the operator's own real
# stores/repo content — never commit; see docs/testing.md WS16).
benchmarks/retrieval/local-queries.jsonl

# Git worktrees
.worktrees/
.superpowers/

# Per-worktree mise local override (P7 renderer-port namespacing).
# Written into each isolated worktree at acquire time; must never be harvested.
.mise.local.toml
mise.local.toml

# AI Orchestrator project memory skeletons
.aio/

# AI Orchestrator local runtime activity
.ao/

# Loop Mode runtime state (the loop coordinator writes these into the
# workspace where it runs; they are per-iteration memory, not source).
# Current loops scope all state under .aio-loop-state/<runId>/; the bare
# root entries below are legacy (older loops wrote them at the workspace root).
/STAGE.md
/NOTES.md
/ITERATION_LOG.md
/LOOP_TASKS.md
/DONE.txt
/BLOCKED.md
# AI Orchestrator loop runtime state
.aio-loop-attachments/
.aio-loop-control/
.aio-loop-state/

# Doc-review HTML artifacts (disposable human-review render target; never committed)
.aio-review/

# Temporary JSON dumps (vitest session reviews, etc.)
.tmp-*.json
*.tmp.json
_scratch/

# Orphaned pre-migration JS copies
cross-model-review-service.js
