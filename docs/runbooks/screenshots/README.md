# Runbook Screenshots

Wave 7 keeps screenshots under `docs/runbooks/screenshots/<wave>/`.

Screenshots should be captured from the packaged app after the automated gates pass, using macOS light and dark appearance at 100% zoom. Do not capture from `npm run dev` when validating packaging-sensitive UI.
